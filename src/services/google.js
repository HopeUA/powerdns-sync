import * as taskHandlers from './google-tasks';

class Google {
    static async doTask(task) {
        // taskHandlers[task.type](task.data);
    }

    static async getZones() {

        return [{
            qwe: 'qq',
            qq: {
                qqqq: "qwe"
            }
        }];
    }
}

export default Google;
